name: 'test'

inputs:
  labels:
    description: 'label name'
    required: false
    default: 'client'
outputs:
  path-variable:
    description: 'sdfasdf'

runs:
  using: 'node16'
  main: './actions-scripts/use-the-api.mjs'
